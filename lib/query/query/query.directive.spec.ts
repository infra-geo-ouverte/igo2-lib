import { QueryDirective } from './query.directive';

describe('QueryDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryDirective();
    expect(directive).toBeTruthy();
  });
});
