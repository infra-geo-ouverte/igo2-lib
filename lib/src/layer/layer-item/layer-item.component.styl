@require '../../../styles.styl';
@require '../layer-legend/layer-legend.component.styl';

:host {
  overflow: hidden;
}

.igo-layer-actions-container,
.igo-layer-legend-container {
  max-width: 100%;
  display: inline-block;
}

.igo-layer-actions-container {
  bordered-top();
  bordered-bottom();
}

.igo-layer-actions-container > div {
  text-align: center;
}

.igo-layer-legend-container {
  padding-left: $igo-layer-legend-padding-left;
}

#opacity-slider {
  width: 100%;

  +media(mobile) {
    width: 70%;
  }
}

.igo-layer-button-group {
  float: right;
  padding: 0 $igo-list-item-padding;

  +media(mobile) {
    float: none;
  }
}
