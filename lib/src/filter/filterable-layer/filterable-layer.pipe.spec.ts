import { FilterableLayerPipe } from './filterable-layer.pipe';

describe('FilterableLayerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterableLayerPipe();
    expect(pipe).toBeTruthy();
  });
});
