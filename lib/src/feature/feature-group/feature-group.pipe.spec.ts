import { FeatureGroupPipe } from './feature-group.pipe';

describe('FeatureGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FeatureGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
