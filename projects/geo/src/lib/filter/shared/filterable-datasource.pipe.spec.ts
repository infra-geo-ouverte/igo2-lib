import { FilterableDataSourcePipe } from './filterable-datasource.pipe';

describe('FilterableDataSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterableDataSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
