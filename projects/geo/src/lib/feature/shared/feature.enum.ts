export enum FeatureType {
  DataSource = 'DataSource',
  Feature = 'Feature',
  Record = 'Record'
}

export enum FeatureFormat {
  GeoJSON = 'GeoJSON',
  JSON = 'JSON'
}

export enum SourceFeatureType {
  Query = 'Query',
  Search = 'Search',
  Click = 'Click',
  LocateXY = 'LocateXY'
}
