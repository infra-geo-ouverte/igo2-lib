@require '../../../style/var.styl';

.igo-zoom-container {
  width: $igo-icon-size;
}

.igo-zoom-container button:first-child {
  margin-bottom: 2px;
}

button,
:host >>> button .mat-button-ripple-round {
  border-radius: 0;
}
