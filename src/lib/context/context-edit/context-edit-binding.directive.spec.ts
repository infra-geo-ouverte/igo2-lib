import { TestBed } from '@angular/core/testing';

describe('ContextEditBindingDirective', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      imports: [],
      providers: []
    });
  });

  it('should create an instance', () => {
    expect(true).toBeTruthy();
  });
});
