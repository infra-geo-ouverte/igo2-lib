@require '../../../style/var.styl';
@require '../../../style/media.styl';
@require '../../shared/collapsible/collapsible.component.styl';

:host {
  overflow: hidden;
}

.igo-actions-container {
  flex-shrink: 0;
}

.igo-actions-expand-container {
  display: inline-flex;
}

md-list-item >>> .mat-list-item-content .mat-list-text {
  padding: 0;
}

md-list-item >>> .mat-list-item-content .mat-list-text > h4 {
  padding: 0 16px;
}

md-icon.disabled {
  color: rgba(0,0,0,.38);
}
