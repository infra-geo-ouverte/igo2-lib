/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OgcFilterChipsComponent } from './ogc-filter-chips.component';

describe('OgcFilterChipsComponent', () => {
  let component: OgcFilterChipsComponent;
  let fixture: ComponentFixture<OgcFilterChipsComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [OgcFilterChipsComponent]
    }).compileComponents();

    fixture = TestBed.createComponent(OgcFilterChipsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
